# cis4930-sp26-django-project-group-21

#Sraddha's temporary README note: I used GitHub's default .gitignore, so it's part of "main" and not "models-orm," be careful when pulling venv/env
#--> The problem should be solved once merged to main, but the requirements dictate at least four branches so wanted to hold off
